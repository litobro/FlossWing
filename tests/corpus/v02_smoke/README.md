# v02_smoke

Tiny pinned Python project for FlossWing v0.2 integration testing.
Recon should be able to identify:
- language: python
- build command: `pip install .`
- one obvious CLI entry point (`example.cli:main`)

Do not extend this repo without updating
`tests/integration/test_recon_smoke.py` accordingly.
